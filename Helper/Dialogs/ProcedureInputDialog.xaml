<Window x:Class="MedicalAir.Helper.Dialogs.ProcedureInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод данных процедуры"
        Icon="pack://application:,,,/icon.png"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F7FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{Binding ProcedureName}" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Foreground="#48bb78"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   Text="{Binding Description}" 
                   TextWrapping="Wrap"
                   FontSize="14" 
                   Foreground="#666"
                   Margin="0,0,0,15"/>

        <TextBlock Grid.Row="2" 
                   Text="Введите значение:" 
                   FontSize="14" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"/>

        <TextBox Grid.Row="3" 
                 x:Name="ValueTextBox"
                 Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" 
                 Padding="10,8"
                 Margin="0,0,0,10"
                 KeyDown="ValueTextBox_KeyDown"/>

        <TextBlock Grid.Row="4" 
                   Text="{Binding RangeText}" 
                   FontSize="12" 
                   Foreground="#999"
                   VerticalAlignment="Top"
                   Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" 
                    Width="100" 
                    Height="35"
                    Background="#48bb78"
                    Foreground="White"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"
                    Click="OkButton_Click" 
                    IsDefault="True"/>
            <Button Content="Отмена" 
                    Width="100" 
                    Height="35"
                    Background="#cbd5e0"
                    Foreground="#333"
                    BorderThickness="0"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Click="CancelButton_Click" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

