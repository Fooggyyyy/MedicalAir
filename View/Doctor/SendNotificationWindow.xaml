<Window x:Class="MedicalAir.View.Doctor.SendNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MedicalAir - Отправка уведомлений"
        Icon="pack://application:,,,/icon.png"
        WindowState="Maximized"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">
    <Grid>
        
        <Border Background="#F3F3F3"
                Height="50"
                VerticalAlignment="Top"
                BorderBrush="#D0D0D0"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" 
                           Orientation="Horizontal" 
                           VerticalAlignment="Center"
                           Margin="20,0,0,0">
                    <TextBlock Text="MedicalAir"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#48bb78"
                               VerticalAlignment="Center"/>
                    <TextBlock Text=" - Отправка уведомлений"
                               FontSize="18"
                               Foreground="#666"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"/>
                </StackPanel>
                
                <Button x:Name="BackButton"
                        Grid.Column="1"
                        Content="🔙 Назад"
                        Width="120"
                        Height="40"
                        Margin="0,0,30,0"
                        Background="#667eea"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="BackButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#667eea"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#5568d3"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <Border Background="White"
                Width="250"
                VerticalAlignment="Stretch"
                HorizontalAlignment="Left"
                Margin="0,70,0,0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
            </Border.Effect>
            <StackPanel Margin="15,20,15,20">
                <TextBlock Text="Навигация"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#48bb78"
                           Margin="0,0,0,20"/>
                
                <Button x:Name="MainPageButton"
                        Content="🏠 Главная"
                        Height="50"
                        Margin="0,0,0,10"
                        Background="#e2e8f0"
                        Foreground="#333"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="MainPageButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#e2e8f0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#cbd5e0"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                
                <Button x:Name="SendNotificationButton"
                        Content="📨 Отправить уведомление"
                        Height="50"
                        Margin="0,0,0,10"
                        Background="#48bb78"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#48bb78"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#38a169"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
        
        <Grid Margin="280,90,20,20" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Border Background="White"
                    Padding="30"
                    CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.1"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Отправка уведомлений"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#48bb78"
                               Margin="0,0,0,30"/>

                    <TextBlock Grid.Row="1"
                               Text="Пилоты:"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#333"
                               Margin="0,0,0,10"/>
                    
                    <Grid Grid.Row="2" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding Pilots}"
                                  SelectedItem="{Binding SelectedPilot}"
                                  DisplayMemberPath="FullName"
                                  Margin="0,0,15,0"
                                  Padding="10,8"
                                  FontSize="14">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="IsEnabled" Value="True"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SendToAllPilots}" Value="True">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                        
                        <CheckBox Grid.Column="1"
                                  Content="Отправить всем пилотам"
                                  IsChecked="{Binding SendToAllPilots}"
                                  VerticalAlignment="Center"
                                  Margin="0,0,15,0"
                                  FontSize="14"/>
                    </Grid>

                    <TextBlock Grid.Row="3"
                               Text="Бортпроводники:"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#333"
                               Margin="0,0,0,10"/>
                    
                    <Grid Grid.Row="4" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding FlightAttendants}"
                                  SelectedItem="{Binding SelectedFlightAttendant}"
                                  DisplayMemberPath="FullName"
                                  Margin="0,0,15,0"
                                  Padding="10,8"
                                  FontSize="14">
                            <ComboBox.Style>
                                <Style TargetType="ComboBox">
                                    <Setter Property="IsEnabled" Value="True"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SendToAllFlightAttendants}" Value="True">
                                            <Setter Property="IsEnabled" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ComboBox.Style>
                        </ComboBox>
                        
                        <CheckBox Grid.Column="1"
                                  Content="Отправить всем бортпроводникам"
                                  IsChecked="{Binding SendToAllFlightAttendants}"
                                  VerticalAlignment="Center"
                                  FontSize="14"/>
                    </Grid>

                    <TextBlock Grid.Row="5"
                               Text="Текст уведомления:"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="#333"
                               Margin="0,0,0,10"/>
                    
                    <TextBox Grid.Row="6"
                             Text="{Binding MessageBody}"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             Height="150"
                             Padding="15"
                             FontSize="14"
                             Margin="0,0,0,20"
                             VerticalScrollBarVisibility="Auto"/>

                    <Button Grid.Row="7"
                            Content="📤 Отправить уведомление"
                            Command="{Binding SendNotification}"
                            Width="250"
                            Height="50"
                            Background="#48bb78"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="16"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            HorizontalAlignment="Right">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#48bb78"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#38a169"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
